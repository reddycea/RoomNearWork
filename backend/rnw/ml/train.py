"""Training entrypoint placeholder for a future ranking model.

Run this after you have enough interaction data. The baseline production system
uses transparent scoring so the project is useful before ML data volume exists.
"""


def main() -> None:
    print("No trained model yet. Use recommendation_service.py baseline scoring first.")


if __name__ == "__main__":
    main()
